import fs from "node:fs";
import path from "node:path";
import process from "node:process";
import { pathToFileURL } from "node:url";
const root=path.resolve(import.meta.dirname,"..");
const files=["index.html","usage-rules.html","ccps-rules.js","ccps-brand-options.js","ccps-ui-options.js","ccps-content-engine.js","ccps-gpt-adapter.js","ccps-multiplatform-engine.js","ccps-external-links.js","ccps-storage.js","ccps-preflight.js","app.js","README.md","docs/CCPS_CONTENT_RULES.md","assets/ccps-logo.jpg","assets/uncle-house-logo.jpeg"];
const text=files.filter(f=>fs.existsSync(path.join(root,f))).map(f=>fs.readFileSync(path.join(root,f),"utf8")).join("\n");
const pageText=["index.html","usage-rules.html"].map(f=>fs.readFileSync(path.join(root,f),"utf8")).join("\n");
const rules=await import(pathToFileURL(path.join(root,"ccps-rules.js")));
const engine=await import(pathToFileURL(path.join(root,"ccps-content-engine.js")));
const gpt=await import(pathToFileURL(path.join(root,"ccps-gpt-adapter.js")));
const preflight=await import(pathToFileURL(path.join(root,"ccps-preflight.js")));
const brandOptions=await import(pathToFileURL(path.join(root,"ccps-brand-options.js")));
const multi=await import(pathToFileURL(path.join(root,"ccps-multiplatform-engine.js")));
const external=await import(pathToFileURL(path.join(root,"ccps-external-links.js")));
const ui=await import(pathToFileURL(path.join(root,"ccps-ui-options.js")));
const checks=[];const check=(name,pass,detail="")=>checks.push({name,pass:Boolean(pass),detail});
files.forEach(f=>check(`file:${f}`,fs.existsSync(path.join(root,f))));
check("7 roles",rules.CONTENT_ROLES.length===7);
check("hook types >= 9",rules.HOOK_TYPES.length>=9);
check("narratives >= 10",rules.NARRATIVE_TYPES.length>=10);
check("anti-cliche",rules.CLICHES.length>=12);
check("single similarity threshold source",Object.keys(rules.SIMILARITY_THRESHOLDS).length===2 && !/0\.25|0\.40/.test(fs.readFileSync(path.join(root,"ccps-preflight.js"),"utf8")));
check("storage namespace",Object.values(rules.STORAGE_KEYS).every(k=>k.startsWith("ccpsOs")));
check("no uncleHouse storage key",!text.includes("uncleHouse"));
check("no secret literal",!/(sk-[A-Za-z0-9_-]{8,}|EA[A-Za-z0-9]{30,})/.test(text));
check("article evidence gate removed",!text.includes("EVIDENCE_REQUIRED")&&!text.includes('id="evidenceState"')&&!text.includes('id="evidence"'));
check("fabricated story gate",text.includes("fabricated_story_gate") && text.includes("FABRICATED_REAL_WORLD_EVENT"));
check("writing fingerprint Chinese",text.includes("CCPS 寫作指紋") && text.includes("儲存寫作指紋")&&!text.includes("Voice Fingerprint"));
check("tracking fields",rules.TRACKING_FIELDS.every(f=>text.includes(f)),rules.TRACKING_FIELDS.filter(f=>!text.includes(f)).join(","));
check("brand CTA",rules.BRAND_CTA==="追蹤ccps家慶佳業" && !/追蹤房叔|房叔自媒體學院/.test(text));
check("14 configurable brand fields",Object.keys(brandOptions.BRAND_FIELD_OPTIONS).length===14);
check("8+ choices per brand field",Object.values(brandOptions.BRAND_FIELD_OPTIONS).every(options=>options.length>=8));
check("brand fields are selects",Object.keys(brandOptions.BRAND_FIELD_OPTIONS).every(id=>text.includes(`<select id="${id}">`)));
check("brand ending locked",text.includes('id="brandEnding" value="追蹤ccps家慶佳業" readonly'));
check("GPT material selector",text.includes('id="selectedMaterial"')&&text.includes("生成時選用素材"));
check("GPT key is page-memory only",text.includes("OpenAI 應用程式金鑰（僅目前分頁，不儲存）")&&!text.includes('writeStore("apiKey"')&&!text.includes("sessionStorage"));
check("Uncle House header logo",text.includes('src="./assets/uncle-house-logo.jpeg"')&&fs.statSync(path.join(root,"assets/uncle-house-logo.jpeg")).size>10000);
check("old CCPS logo preserved",fs.existsSync(path.join(root,"assets/ccps-logo.jpg")));
check("homepage title",text.includes("<title>UHOS 自媒體工廠</title>")&&text.includes("<h1>UHOS 自媒體工廠</h1>")&&!pageText.includes("UHOS ccps自媒體工廠"));
check("self media generator first",text.includes('id="contentGenerator"><h2>自媒體產生器</h2>')&&text.indexOf('id="contentGenerator"')<text.indexOf('id="homeQuickEntry"'));
check("hamburger menu",text.includes('id="menuToggle"')&&text.includes('id="menuDrawer"')&&text.includes('aria-label="功能目錄"'));
check("menu contains work sections",["companyLinks","adsLinks","quickLinks","recentPosts"].every(id=>text.includes(`id="${id}"`))&&text.indexOf('id="menuDrawer"')<text.indexOf('id="companyLinks"')&&text.indexOf('id="recentPosts"')<text.indexOf("</aside>"));
check("recent posts only in menu",(text.match(/id="postList"/g)||[]).length===1&&text.indexOf('id="postList"')<text.indexOf("</aside>"));
check("home quick entry",text.includes('id="homeQuickEntry"')&&text.includes('id="openQuickMenu"'));
check("copyright and usage rules",text.includes("© 2026 房叔 UHOS 系統｜保留所有權利")&&text.includes('href="https://jay0935927053-sudo.github.io/ccps-ai-overseas-property-content-os/usage-rules.html"')&&text.includes("房叔 UHOS 系統使用規則"));
check("Chinese operation labels",["臉書信任文章","品牌與寫作語氣","開場方式","敘事結構","應用程式金鑰","內容安全檢查","重新生成不同內容指紋","觸及人數","成效與質性備註"].every(x=>text.includes(x)));
check("English operation labels removed",!["Evidence State","Freshness 檢查","Narrative Type","API Key","Qualitative Notes","<label>Reach</label>","<label>Reactions</label>"].some(x=>pageText.includes(x)));
check("evidence Chinese display",Object.keys(ui.EVIDENCE_LABELS).length===3&&rules.EVIDENCE_STATES.every(x=>ui.EVIDENCE_LABELS[x]&&!ui.EVIDENCE_LABELS[x].includes("_")));
check("material select fields",["materialTitle","materialSource","materialRegion","materialProperty","materialClient","materialPrivacy","materialContentTemplate","officialTopic"].every(id=>text.includes(`<select id="${id}"`)));
check("material option coverage",ui.MATERIAL_FORM_OPTIONS.titles.length>=8&&ui.MATERIAL_FORM_OPTIONS.sources.length>=8&&ui.MATERIAL_FORM_OPTIONS.regions.length>=8&&ui.MATERIAL_FORM_OPTIONS.properties.length>=8&&ui.MATERIAL_FORM_OPTIONS.clients.length>=8&&ui.MATERIAL_FORM_OPTIONS.privacy.length>=6&&ui.MATERIAL_FORM_OPTIONS.tags.length>=12);
check("material templates",ui.MATERIAL_CONTENT_TEMPLATES.length>=6&&ui.MATERIAL_CONTENT_TEMPLATES.every(x=>x.label&&x.body!==undefined));
check("search topic choices",ui.MATERIAL_FORM_OPTIONS.searchTopics.length>=12&&ui.MATERIAL_FORM_OPTIONS.searchTopics[0]==="全部主題");
check("material usage guide",text.includes("素材庫怎麼使用？")&&text.includes("先選欄位")&&text.includes("勾選標籤")&&text.includes("套用內容範本")&&text.includes("判斷能否公開"));
check("4 platform asset types",multi.PLATFORM_ASSET_TYPES.length===4);
check("video durations 15 30 60",JSON.stringify(multi.VIDEO_DURATIONS)==="[15,30,60]");
check("8+ video styles",multi.VIDEO_STYLES.length>=8);
check("4 aspect ratios",multi.ASPECT_RATIOS.length>=4);
check("GPT image link",external.EXTERNAL_LINK_GROUPS.image.some(x=>x.url==="https://chatgpt.com/"));
check("ChatArt Pro video link",external.EXTERNAL_LINK_GROUPS.video.length===1&&external.EXTERNAL_LINK_GROUPS.video[0].url==="https://app.chatartpro.com/");
check("homepage AI Director link",external.EXTERNAL_LINK_GROUPS.aiDirector.length===1&&external.EXTERNAL_LINK_GROUPS.aiDirector[0].label==="AI Director"&&external.EXTERNAL_LINK_GROUPS.aiDirector[0].url==="https://uhos-ai-director.unclehouse.chatgpt.site/"&&text.includes('id="homeQuickEntry"'));
check("3 CCPS company links",external.EXTERNAL_LINK_GROUPS.company.length===3);
check("CCPS company link coverage",["https://ccps-my.com/","https://glorious-curiosity-production-964b.up.railway.app/login","https://ccps-airline-webinar.unclehouse.chatgpt.site/admin/leads"].every(url=>external.EXTERNAL_LINK_GROUPS.company.some(x=>x.url===url)));
check("independent ads manager link",external.EXTERNAL_LINK_GROUPS.ads.length===1&&external.EXTERNAL_LINK_GROUPS.ads[0].url==="https://adsmanager.facebook.com/"&&text.includes('id="adsLinks"'));
check("ad operator button",external.EXTERNAL_LINK_GROUPS.adOperator.length===1&&external.EXTERNAL_LINK_GROUPS.adOperator[0].url==="https://growth-staging.unclehouse.net/"&&text.includes('id="adOperatorButton"')&&text.includes('rel="noopener noreferrer">廣告投手</a>'));
check("webinar admin button",external.EXTERNAL_LINK_GROUPS.webinarAdmin.length===1&&external.EXTERNAL_LINK_GROUPS.webinarAdmin[0].label==="說明會後台"&&external.EXTERNAL_LINK_GROUPS.webinarAdmin[0].url==="https://ccps-airline-webinar.unclehouse.chatgpt.site/admin"&&text.includes('id="webinarAdminButton"')&&text.includes('href="https://ccps-airline-webinar.unclehouse.chatgpt.site/admin" target="_blank" rel="noopener noreferrer">說明會後台</a>')&&text.indexOf('id="adOperatorButton"')<text.indexOf('id="webinarAdminButton"'));
check("independent company section",text.includes('id="companyLinks"')&&text.indexOf('id="companyLinks"')<text.indexOf('id="adsLinks"')&&text.indexOf('id="adsLinks"')<text.indexOf('id="quickLinks"'));
check("6 social links",external.EXTERNAL_LINK_GROUPS.social.length===6);
check("CCPS social account links",["https://www.facebook.com/CcpsmyInvestmentig","https://www.instagram.com/ccpsmy_investment/","https://www.youtube.com/@ccpsmy-investment","https://www.tiktok.com/zh-Hant-TW/","https://manager.line.biz/account/@392okwmj","https://www.threads.com/@ccpsmy_investment"].every(url=>external.EXTERNAL_LINK_GROUPS.social.some(x=>x.url===url)));
check("external link allowlist",Object.values(external.EXTERNAL_LINK_GROUPS).flat().every(x=>external.isApprovedExternalUrl(x.url))&&!external.isApprovedExternalUrl("https://example.com/"));
check("safe new tab links",text.includes('window.open(url,"_blank","noopener,noreferrer")'));
const base={topic:"MM2H 政策",content_role:"專業判斷",hook_type:"直接結論",narrative_type:rules.NARRATIVE_TYPES[1],visual_type:"真實文件",CTA_type:rules.BRAND_CTA,comment_question:"你最想知道什麼？"};
check("direct generation",!engine.buildArticle(base,[]).blocked);
const fake=preflight.runPreflight({...base,body:"昨天有位客戶問我們怎麼判斷"},[]);
check("fake story blocks",fake.fabricated_story_gate==="BLOCK: FABRICATED_REAL_WORLD_EVENT");
check("freshness is advisory",preflight.runPreflight({...base,body:"馬來西亞政策整理"},[]).freshness_warning==="REVIEW_RECOMMENDED");
const good=engine.buildArticle(base,[]);
check("article output",!good.blocked && good.body.includes(rules.BRAND_CTA) && good.checks.brand_pov==="PASS");
const promptForm={topic:"TEST_TOPIC",category:"TEST_CATEGORY",content_role:"TEST_ROLE",hook_type:"TEST_HOOK",narrative_type:"TEST_NARRATIVE",goal:"TEST_GOAL",visual_type:"TEST_VISUAL",comment_question:"TEST_QUESTION",CTA_type:rules.BRAND_CTA};
const promptBrand={brandPosition:"TEST_POSITION",brandArea:"TEST_AREA",brandAudience:"TEST_AUDIENCE",brandTone:"TEST_BRAND_TONE",brandPrinciples:"TEST_BRAND_PRINCIPLES",brandForbidden:"TEST_BRAND_FORBIDDEN"};
const promptVoice={voiceTone:"TEST_VOICE_TONE",voiceColloquial:"TEST_COLLOQUIAL",voicePerson:"TEST_PERSON",voiceWords:"TEST_WORDS",voiceForbidden:"TEST_VOICE_FORBIDDEN",voiceNever:"TEST_NEVER",voicePrinciples:"TEST_VOICE_PRINCIPLES",voiceExamples:"TEST_EXAMPLES"};
const promptMaterial={id:"material-1",title:"TEST_MATERIAL_TITLE",body:"TEST_MATERIAL_BODY",source:"TEST_MATERIAL_SOURCE",source_date:"2026-08-26",evidence_state:"MEMORY_DERIVED",freshness_required:true,privacy_note:"無個資，可作公開內容候選"};
const prompt=gpt.buildUserPrompt({form:promptForm,brand:promptBrand,voice:promptVoice,material:promptMaterial});
check("GPT prompt 8 article fields",Object.values(promptForm).filter(value=>value!==rules.BRAND_CTA).every(value=>prompt.includes(value)));
check("GPT prompt 6 brand fields",Object.values(promptBrand).every(value=>prompt.includes(value)));
check("GPT prompt 8 voice fields",Object.values(promptVoice).every(value=>prompt.includes(value)));
check("GPT prompt selected material",[promptMaterial.title,promptMaterial.body,promptMaterial.source,promptMaterial.source_date,promptMaterial.evidence_state,"需要重新查證"].every(value=>prompt.includes(value)));
check("GPT prompt fixed CTA",prompt.includes(rules.BRAND_CTA));
check("GPT choices are instructions",prompt.includes(`必須依「文章角色：${promptForm.content_role}」`)&&prompt.includes(`採用「開場方式：${promptForm.hook_type}」`)&&prompt.includes(`依「敘事結構：${promptForm.narrative_type}」`));
const noMaterialPrompt=gpt.buildUserPrompt({form:promptForm,brand:promptBrand,voice:promptVoice,material:null});
check("GPT no unselected material",!noMaterialPrompt.includes(promptMaterial.body)&&noMaterialPrompt.includes("本次未選用素材"));
const blockedMaterials=[
  ...gpt.BLOCKED_MATERIAL_PRIVACY.map(privacy_note=>({...promptMaterial,privacy_note})),
  {...promptMaterial,privacy_note:"",visibility:"internal_only"},
  {...promptMaterial,privacy_note:"",visibility:"UNKNOWN"}
];
check("GPT 6 material cloud blocks",blockedMaterials.length===6&&blockedMaterials.every(material=>!gpt.materialCloudEligibility(material).pass));
let fetchCalls=0,capturedRequest=null;
const streamText=`data: ${JSON.stringify({type:"response.output_text.delta",delta:`CCPS 判斷：${promptForm.topic}需要先比較條件。\n\n${rules.BRAND_CTA}`})}\n\ndata: [DONE]\n\n`;
const streamBytes=new TextEncoder().encode(streamText);
const mockFetch=async(url,options)=>{fetchCalls+=1;capturedRequest={url,options};let sent=false;return {ok:true,status:200,body:{getReader:()=>({read:async()=>sent?{done:true}:{done:(sent=true)&&false,value:streamBytes}})}}};
const mockOutput=await gpt.requestGptArticle({apiKey:"TEST_KEY_DO_NOT_STORE",form:promptForm,brand:promptBrand,voice:promptVoice,material:promptMaterial,fetchImpl:mockFetch});
const capturedBody=JSON.parse(capturedRequest.options.body);
check("GPT mock single request",fetchCalls===1&&capturedRequest.url===gpt.GPT_ENDPOINT&&capturedRequest.options.method==="POST");
check("GPT request config",capturedBody.model===gpt.GPT_MODEL&&capturedBody.reasoning.effort==="low"&&capturedBody.max_output_tokens===8000&&capturedBody.stream===true);
check("GPT secret not in request content",!capturedRequest.url.includes("TEST_KEY_DO_NOT_STORE")&&!capturedRequest.options.body.includes("TEST_KEY_DO_NOT_STORE")&&!prompt.includes("TEST_KEY_DO_NOT_STORE"));
check("GPT mock stream output",mockOutput.includes(promptForm.topic)&&mockOutput.endsWith(rules.BRAND_CTA));
const gptArticle=engine.buildArticleFromGpt(promptForm,mockOutput,[]);
check("GPT article keeps downstream contract",!gptArticle.blocked&&gptArticle.generation_source==="OPENAI_RESPONSES_API"&&gptArticle.body.includes(rules.BRAND_CTA)&&multi.buildVideoPackage(gptArticle,{duration:15}).includes(rules.BRAND_CTA));
let blockedFetchCalls=0;
for(const material of blockedMaterials){try{await gpt.requestGptArticle({apiKey:"TEST_KEY",form:promptForm,brand:promptBrand,voice:promptVoice,material,fetchImpl:async()=>{blockedFetchCalls+=1;return {ok:true}}})}catch{}}
check("GPT material blocks before fetch",blockedFetchCalls===0);
const errorCode=async(args)=>{try{await gpt.requestGptArticle({apiKey:"TEST_KEY",form:promptForm,brand:promptBrand,voice:promptVoice,material:null,...args});return "NO_ERROR"}catch(error){return error.code}};
check("GPT distinct failure states",await errorCode({apiKey:""})==="MISSING_API_KEY"&&await errorCode({fetchImpl:async()=>{throw new Error("offline")}})==="API_NETWORK_ERROR"&&await errorCode({fetchImpl:async()=>({ok:false,status:429})})==="API_RESPONSE_ERROR"&&await errorCode({fetchImpl:async()=>({ok:true,status:200,body:{getReader:()=>({read:async()=>({done:true})})}})})==="EMPTY_API_OUTPUT");
check("FB image prompt",multi.buildFacebookImagePrompt(good).includes("FB 單張圖片提示詞"));
check("IG exactly 5 slides",(multi.buildInstagramCarouselPrompts(good).match(/第 [1-5] 張｜/g)||[]).length===5);
check("YouTube thumbnail prompt",multi.buildYoutubeThumbnailPrompt(good).includes("1280×720"));
check("15 30 60 video packages",multi.VIDEO_DURATIONS.every(duration=>multi.buildVideoPackage(good,{duration}).includes(`【${duration} 秒影片腳本與逐鏡分鏡】`)));
check("storyboard production fields",["畫面：","鏡位：","字幕：","口白：","分鏡圖提示詞："].every(x=>multi.buildVideoPackage(good,{duration:30}).includes(x)));
check("multiplatform CTA",multi.buildInstagramCarouselPrompts(good).includes(rules.BRAND_CTA)&&multi.buildVideoPackage(good,{duration:60}).includes(rules.BRAND_CTA));
const requiredUi=["生成文章","內容安全檢查","重新生成不同內容指紋","儲存文章","複製","下載","素材庫","內容月曆","成效回填"];
check("required UI",requiredUi.every(x=>text.includes(x)),requiredUi.filter(x=>!text.includes(x)).join(","));
checks.forEach(c=>console.log(`${c.pass?"PASS":"FAIL"} ${c.name}${c.detail?` — ${c.detail}`:""}`));
const failed=checks.filter(c=>!c.pass);console.log(`\n${checks.length-failed.length}/${checks.length} PASS`);process.exit(failed.length?1:0);
